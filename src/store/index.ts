import Vue from 'vue'
import Vuex from 'vuex'
import stages from './stages'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: { stages }
})
